server:
  port: 9092
  tomcat:
    max-threads: 1024
    uri-encoding: utf-8

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/antun?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
  redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: localhost
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password:
    #pool:
      # 连接池最大连接数（使用负值表示没有限制）
      #max-active: 8
      # 连接池最大阻塞等待时间（使用负值表示没有限制）
      #max-wait: -1
      # 连接池中的最大空闲连接
      #max-idle: 8
      # 连接池中的最小空闲连接
      #min-idle: 0
    # 连接超时时间（毫秒）
    timeout: 6000
mybatis:
  mapper-locations: mapper/*.xml
  type-aliases-package: com.zdjy.bigdata.antun.mapper
  type-handlers-package: org.apache.ibatis.type.LocalDateTypeHandler
  
logging:
  level:
    root: info
    com.zdjy.bigdata.antun.scheduler.PingAnSendingScheduler: debug
    com.zdjy.bigdata.antun.scheduler.StatisticScheduler: debug
    com.zdjy.bigdata.antun.remote.PingAnSending: debug
    com.zdjy.bigdata.antun.web.UserController: debug
    com.zdjy.bigdata.antun.mapper: debug
pingan:
  sending:
    url: http://47.94.250.65/pinganjiekou/user/add
    channelCode: afangti_zhangrui_001
    channelToken: 26246952-113-11343-3510060-1131-46-46-91114